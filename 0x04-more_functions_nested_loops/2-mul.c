#include "main.h"

/**
 * mul - Function entry
 * Description: A function that multiplies two integers
 * @a: the variable that holds the value to check
 * @b: the variable that holds the value to check
 * Return: Returns 1 if c is a digit, 0 otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
